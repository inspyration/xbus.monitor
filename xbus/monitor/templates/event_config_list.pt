<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en"
    xmlns:tal="http://xml.zope.org/namespaces/tal"
    xmlns:metal="http://xml.zope.org/namespaces/metal"
    xmlns:i18n="http://xml.zope.org/namespaces/i18n"
    i18n:domain="xbus.monitor">
  <head>
      <span metal:use-macro="macros['head-base']" />
  </head>

  <body>
    <div>List of all event types with (name+description) with an edit/delete button for each as well as a create button.</div>
    <div><a href="/config/event/new">Create</a></div>
    <table id="event_list" class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Name</th>
                <th>Description</th>
                <th><span class="glyphicon glyphicon-edit"></span></th>
                <th><span class="glyphicon glyphicon-trash"></span></th>
            </tr>
        </thead>
        <tbody>
            <tal:loop tal:repeat="event events">
                <tr>
                    <td><a href="/html/config/event/${event.id}">${event.name}</td>
                    <td>${event.description}</td>
                    <td><a href="/html/config/event/${event.id}/edit"><span class="glyphicon glyphicon-edit"></span></a></td>
                    <td><form id="deleteform${event.id}" method="POST" action="/html/config/event/${event.id}/delete"><input type="submit" class="glyphicon glyphicon-trash" /></form></td>
                </tr>
            </tal:loop>
        </tbody>
    </table>
    <!-- Bootstrap core JavaScript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <span metal:use-macro="macros['scripts-base']" />
  </body>
</html>
